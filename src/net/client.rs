use std::io::prelude::*;
use std::net::TcpStream;

struct BasicPaxosClient {
    tcpStream: TcpStream,
}

impl BasicPaxosClient {
    pub fn approve(proposalId: u64) {

    }

    fn onAccept() {

    }
}
